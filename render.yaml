services:
  - type: web
    name: www-bukuyatrackdriverssacco
    runtime: static
    buildCommand: npm run build || npm install
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
